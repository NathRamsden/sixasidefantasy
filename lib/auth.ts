// lib/auth.ts – landing-only branch stub
// We don't need real auth logic on the coming-soon site.

export async function getCurrentUser() {
  // On this branch we always behave as if no one is logged in.
  return null;
}
